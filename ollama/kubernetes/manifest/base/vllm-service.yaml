apiVersion: v1
kind: Service
metadata:
  name: ollama-service  # Keep same service name for compatibility
  namespace: open-webui
spec:
  selector:
    app: vllm  # Points to vLLM instead of ollama
  ports:
  - protocol: TCP
    port: 11434
    targetPort: 11434
  type: ClusterIP
